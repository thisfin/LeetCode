//
// Created by 李毅 on 2021/2/26.
//

#include <string>
#include <vector>

using namespace std;

class Solution {
public:
    vector<int> findNumOfValidWords(vector<string>& words, vector<string>& puzzles) {
        vector<int> a;
        return a
    }

    int toInt(word string) {
        for (char c: string) {

        }
        return 0;
    }

    int cToInt(char c) {
        return (int)c - 97;
    }
};
